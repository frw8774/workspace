# 位运算符

位运算符包括逻辑运算符和移位运算符

## 逻辑运算符
逻辑运算符包括 &（按位与）、|（按位或）、^（按位异或）、~（按位取反）四种

注意异或是不同为1，相同为0

## 移位运算符
移位运算符包括<<（左移运算符）、>>（右移运算符）

左移运算符是数的所有位向左移n位，右侧补零（等价于乘上2的n次方）

右移运算符是向右移n位，对于无符号数左侧补零（等价于除以2的n次方），对于有符号数左侧补符号位（正数补零，负数补一）